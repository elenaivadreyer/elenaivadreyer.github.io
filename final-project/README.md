Frontend copy of final-project notebook data.

Source files live under `data/pipeline/final-project/` and are mirrored here so
SvelteKit can serve them to the browser at runtime. The component reads the flat
files in this directory plus the `overlays/` subfolders.
